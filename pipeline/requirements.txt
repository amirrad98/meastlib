pymupdf>=1.24
Pillow>=10.0
requests>=2.31
lxml>=5.0
# OCR engines — install the ones you use:
# kraken            (pip install kraken ; then: kraken get 10.5281/zenodo.7050270 or OpenITI models)
# pytesseract       (plus system tesseract with ara+fas traineddata)
# anthropic/openai/google-generativeai  (for VLM OCR in benchmark)
python-Levenshtein>=0.25
numpy>=1.26
